# Project metadata only. Use the pinned environment in README.md and run from source.
# No build backend, wheel, CLI entry point, or pip-installable package is declared.
[project]
name = "machine-learning-closure-prism"
version = "1.0.6rc1"
description = "Machine learning closure for polymer reference interaction site model (PRISM) integral equation theory"
readme = "README.md"
requires-python = "==3.9.23"
authors = [{ name = "Zhihao Feng" }]
license = "MIT"
license-files = ["LICENSE"]
keywords = ["PRISM", "polymer", "integral equation theory", "machine learning", "closure"]

[project.urls]
Repository = "https://github.com/fengzhihao77/machine-learning-closure-prism"
